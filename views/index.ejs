<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white" >
            <form action="/create" method="post">
                <input class=" block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none" type="text" placeholder="Enter title" name="title">
                <textarea  name="details" class=" block px-5 py-2 bg-zinc-800 w-full rounded-md outline-none  resize-none mt-2"  placeholder="write your task detail here" ></textarea>
                <input type="submit" value="Create Task" class="mt-2 bg-blue-600 px-5 py-2 rounded-md">
            </form>

        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){%>
                    <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md ">
                        <h1 class="text-white text-2xl tracking-tigher">
                          <%= val %>
                        </h1>
                        <a href="/file/<%=val%>" class="text-blue-500 inline-block mt-3 ">Read More.</a>
                        <a href="/file/<%=val%>" class="text-blue-500 inline-block mt-3 ">edit</a>
                        <a href="/delete/<%=val%>" class="inline-block text-red-500 ml-16">delete</a>
                    </div>
                    <% }) %>

          <%  }else{ %>
            <h3 class="text-green-500 text-4xl">No task yet</h3>
            <%}%>
       
          
        </div>
      


    </div>
    

</body>
</html>